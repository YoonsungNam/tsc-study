// SvelteKit 앰비언트 타입 선언.
// Phase 1에서 Locals(인증 사용자), PageData 등을 채울 예정.

declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
